# DevEx Calculator

Robux ↔ USD conversion tool built with Go and HTMX.

## Run (Docker)

```bash
git clone https://github.com/redmonkye/devex-calculator.git
cd devex-calculator
docker build -t devex-calculator .
docker run -p 8080:8080 devex-calculator
```
